---
title: "AI Engine Matchmaker"
description: "At Shapes.inc, we offer a variety of models for running your Shapes. We regularly update the availability of models. Each model has its own strengths and characteristics. Let's break down the options!"
---

### Key:

Thinking tags supported 🧠\
Roleplay 🎭\
Intelligence 📚\
Skills (enabled for tool use)  🛠️\
Human-Like 👤\
Images 🖼️

import { useState } from "react";


<EngineFilter />

export const EngineFilter = () => {
    const engines = [
    { name: "ChatGPT-4.1 Nano", tier: "free", tags: ["📚"], description: "Perfect fallback engine for GPT 4.1 users. Smart and efficient for quick tasks." },
    { name: "ChatGPT-4o Mini", tier: "free", tags: ["🎭","📚","👤"], description: "Fast GPT option — great fallback engine for general use." },
    { name: "ChatGPT-5 Nano", tier: "free", tags: ["🎭","📚"], description: "Low latency with huge context support. A great lightweight GPT option." },
    { name: "DeepSeek V3.2", tier: "free", tags: ["🎭","👤"], description: "Great for creative storytelling, expressive characters, and immersive in-character interactions." },
    { name: "Gemini 2.5 Flash Lite", tier: "free", tags: ["🧠","👤","🛠️"], description: "Amazing multilingual model; great fallback for non-English Shapes." },
    { name: "Gemini 3.1 Flash Lite (Preview)", tier: "free", tags: ["🧠","📚","🛠️"], description: "Google's most cost-efficient model in the Gemini 3 series. Blazing fast with solid reasoning — ideal for high-volume tasks, translation, and as a responsive fallback engine." },
    { name: "Gemma 3 27B", tier: "free", tags: ["🎭"], description: "Large context, balanced performance and latency. A solid option for general roleplay." },
    { name: "GLM 4.7 Flash", tier: "free", tags: ["🧠","🎭","🛠️"], description: "A fast, efficient model from ZAI with built-in thinking capabilities. Excels at creative writing and multilingual tasks while staying lightweight and responsive." },
    { name: "Grok 3 Mini", tier: "free", tags: ["🧠","🎭","📚","🛠️","👤"], description: "Fast, smart, and versatile — the Swiss army knife of free engines." },
    { name: "Grok 4.1 Fast", tier: "free", tags: ["🎭","📚","🛠️"], description: "xAI's agentic powerhouse with a massive 2M token context window. Reduced hallucinations, strong tool use, and surprisingly capable for a free engine. Great for both roleplay and complex tasks." },
    { name: "Llama 3.1 70B", tier: "free", tags: ["🎭","🛠️"], description: "A solid open-source foundation model. Reliable for roleplay and tool use." },
    { name: "Llama 3.3 70B", tier: "free", tags: ["🎭","🛠️"], description: "Multilingual and instruction-tuned. A reliable open-source workhorse." },
    { name: "Llama 4 Maverick", tier: "free", tags: ["🎭","📚","🛠️"], description: "Meta's 17B expert mixture, multimodal model. Versatile and skill-friendly." },
    { name: "Lunaris L3 8B", tier: "free", tags: ["🎭"], description: "Fast, wild, short-form roleplay model. Great for creative roleplays." },
    { name: "Mistral Small 2603", tier: "free", tags: ["🧠","🎭","📚","🛠️"], description: "Mistral's latest hybrid model — combines reasoning, multimodal understanding, and coding in one fast, efficient package. Punches way above its weight class." },

    { name: "ChatGPT-5.4", tier: "premium", tags: ["🧠","📚","🛠️"], description: "Strong general-purpose reasoning model with solid instruction following and tool use." },
    { name: "ChatGPT-5.4 Mini", tier: "premium", tags: ["🎭","📚","🛠️"], description: "A balanced, cost-effective GPT option that handles both complex tasks and casual chat well." },
    { name: "ChatGPT-5.5 Chat", tier: "premium", tags: ["🎭","👤"], description: "Optimized for conversational flow and character interaction, prioritizing natural dialogue over deep analysis." },
    { name: "Claude Haiku 4.5", tier: "premium", tags: ["👤","🛠️"], description: "Fast, expressive, and natural-feeling. Ideal for conversational Shapes that need warmth, personality, and responsiveness without heavy reasoning." },
    { name: "Claude Opus 4.6", tier: "premium", tags: ["📚","🛠️"], description: "Anthropic's most powerful model. Built for long, complex workflows and demanding coding tasks. This is probably the smartest engine available today, and is great for roleplaying also." },
    { name: "Claude Sonnet 4.6", tier: "premium", tags: ["🎭","📚","🛠️"], description: "Anthropic's latest Sonnet. Strong instruction following, coding, and roleplay with excellent personality retention." },
    { name: "Gemini 2.5 Pro", tier: "premium", tags: ["🎭","📚","👤","🛠️"], description: "Google's advanced model for reasoning, coding, and scientific tasks. Great all-rounder." },
    { name: "Gemini 3 Flash Preview", tier: "premium", tags: ["📚","👤","🛠️"], description: "A fast Gemini 3 variant that balances intelligence with conversational speed for interactive Shapes." },
    { name: "Gemini 3.1 Pro Preview", tier: "premium", tags: ["🧠","📚","🛠️"], description: "Google's state-of-the-art reasoning model for complex logic, coding, and long-form thinking." },
    { name: "GLM 5", tier: "premium", tags: ["🎭","🛠️"], description: "The latest flagship from ZAI. Strong roleplay and tool use capabilities." },
    { name: "Kimi K2 0905", tier: "premium", tags: ["🎭","📚","👤"], description: "A versatile model from MoonshotAI with strong roleplay, intelligence, and a natural conversational feel." },
    { name: "Kimi K2.5", tier: "premium", tags: ["🧠","🎭","📚","🛠️"], description: "MoonshotAI's most intelligent model yet — a native multimodal beast with thinking mode, agent capabilities, and strong coding. Excels at complex reasoning and creative tasks alike." },
    { name: "Nous: Hermes 4 405B", tier: "premium", tags: ["🧠","🎭","🛠️"], description: "A large-scale model built for creative roleplay and tool use. Expect deep, expressive responses — can be a bit slow due to its size, but the quality is worth the wait." },
  ];
  const allTags = [...new Set(engines.flatMap(e => e.tags))];
  const [selectedTag, setSelectedTag] = useState([]);

  const tiers = ["premium", "free"];
  const sectionTitle = {
    premium: "Premium Engines",
    free: "Free Engines"
  };

  const sectionStyle = { marginBottom: "30px" };
  const listItemStyle = {
    marginBottom: "10px",
    padding: "10px",
    borderRadius: "5px",
  };

const filtered = selectedTag.length
  ? engines.filter(e => selectedTag.every(tag => e.tags.includes(tag)))
  : engines;

 return (
    <div>
      <div style={{ marginBottom: "15px" }}>
  {allTags.map(tag => (
    <button
      key={tag}
      onClick={() => {
        setSelectedTag(prev =>
          prev.includes(tag) ? prev.filter(t => t !== tag) : [...prev, tag]
        );
      }}
      style={{
        margin: "3px",
        padding: "5px 10px",
        borderRadius: "5px",
        border: "1px solid #007bff",
        cursor: "pointer",
        backgroundColor: selectedTag.includes(tag) ? "#007bff" : "transparent",
        color: selectedTag.includes(tag) ? "#fff" : "#007bff",
      }}
    >
      {tag}
    </button>
  ))}
</div>


      {tiers.map(tier => {
        const group = filtered.filter(e => e.tier === tier);
        if (group.length === 0) return null;

        return (
          <section key={tier} style={sectionStyle}>
            <h3 style={{
              marginBottom: "28px",
              fontSize: "28px",
            }}>
              {sectionTitle[tier]}
            </h3>
            <ul style={{ listStyle: "none", paddingLeft: 0 }}>
              {group.map(engine => (
                <li
                  key={engine.name}
                  style={listItemStyle}
                >
                  <strong>{engine.name}</strong> [{engine.tags.join(", ")}]<br />
                  {engine.description || <em style={{ color: "#777" }}>No description available.</em>}
                </li>
              ))}
            </ul>
          </section>
        );
      })}
    </div>
  );
};

[Return to App](https://shapes.inc)
