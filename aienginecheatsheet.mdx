---
title: "AI Engine Matchmaker"
description: "At Shapes.inc, we offer a variety of models for running your Shapes. We regularly update the availability of models. Each model has its own strengths and characteristics. Let's break down the options!"
---

import { useState } from "react";

<EngineFilter />

export const EngineFilter = () => {
  const engines = [
    { name: "Anthropic: Claude Sonnet 4.5", tier: "premium", tags: ["🧠","🎭","📚","🛠️"], description: "Favorite amongst roleplayers, 4.5 is intelligent, great for tool use, and Shape's default premium engine." },
    { name: "OpenAI: GPT-5", tier: "premium", tags: ["📚","🛠️"], description: "Top-tier reasoning, multimodal." },
    { name: "Gemini 2.5 Pro (Latest)", tier: "premium", tags: ["🎭","📚","👤","🛠️"], description: "Google’s state-of-the-art AI model for advanced reasoning and creativity." },
    { name: "OpenAI: GPT-5 Mini", tier: "premium", tags: ["🎭","📚","🛠️"], description: "Balance of cost & capability." },
    { name: "Anthropic: Claude Sonnet 3.5", tier: "free", tags: ["🎭","📚","🛠️"], description: "Older, cheaper option — still wonderful for roleplays." },
    { name: "OpenAI: GPT-4o-mini", tier: "free", tags: ["🎭","📚","👤"], description: "Fast GPT option, fallback for offline models." },
    { name: "Gemini 2.5 Flash", tier: "free", tags: ["🎭","📚","👤","🛠️"], description: "Fast, multilingual, high performance." },
    { name: "DeepSeek v3 0324", tier: "free", tags: ["🎭","📚","🛠️"], description: "DeepSeek's flagship chat model." }
  ];

  const tiers = ["premium", "free"];

  const sectionTitle = {
    premium: "🌟 Premium Engines",
    free: "💬 Free Engines"
  };

  const sectionStyle = {
    marginBottom: "30px",
  };

  const listItemStyle = {
    marginBottom: "10px",
    padding: "10px",
    borderRadius: "5px",
    background: "#f9f9f9",
    transition: "background 0.2s",
  };

  return (
    <div>
      {tiers.map(tier => {
        const group = engines.filter(e => e.tier === tier);
        return (
          <section key={tier} style={sectionStyle}>
            <h3 style={{ marginBottom: "10px", color: tier === "premium" ? "#007bff" : "#555" }}>
              {sectionTitle[tier]}
            </h3>
            <ul style={{ listStyle: "none", paddingLeft: 0 }}>
              {group.map(engine => (
                <li
                  key={engine.name}
                  style={listItemStyle}
                  onMouseEnter={(e) => e.currentTarget.style.background = "#eef4ff"}
                  onMouseLeave={(e) => e.currentTarget.style.background = "#f9f9f9"}
                >
                  <strong>{engine.name}</strong> [{engine.tags.join(", ")}]<br />
                  {engine.description || <em style={{ color: "#777" }}>No description available.</em>}
                </li>
              ))}
            </ul>
          </section>
        );
      })}
    </div>
  );
};
